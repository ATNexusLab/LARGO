# Card

Componente Card — contêiner com sombra e padding padrão.

Uso
```tsx
import Card from './react/Card';

<Card>
  <h3>Resumo</h3>
  <p>Conteúdo do card</p>
</Card>
```

Observações
- Usa `--color-surface` e box-shadow definidos em `docs/design/generated/ui-styles.css`.
