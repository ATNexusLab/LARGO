# Input

Componente Input — estilo base para formulários.

Props
- Suporta todas as props nativas de `<input>`.

Uso
```tsx
import Input from './react/Input';

<Input placeholder="Digite aqui" />
```

Observações
- Estilos referenciam `--color-surface` e `--color-muted`.
