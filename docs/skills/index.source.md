# Skills

Este documento mapeia as skills presentes em `skills-lock.json` e fornece recomendações de uso e exemplos.

Skills detectadas atualmente (ver `skills-lock.json`):
- shadcn — UI components (shadcn/ui)
- skill-creator — criar novas skills
- ui-ux-pro-max — diretrizes UI/UX e tokens
- vercel-react-best-practices — padrões para React

Para cada skill, documentar: propósito, quando usar, exemplos mínimos de invocação e localização (se aplicável).

Próximos passos:
- Auditar código para encontrar chamadas a skills e ligar a documentação aos pontos de uso.
